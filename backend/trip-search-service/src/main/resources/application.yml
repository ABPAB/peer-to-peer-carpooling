server:
  port: 8081

spring:
  application:
    name: RIDE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/tripDB
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
